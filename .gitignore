# Crawler tooling — not part of the docs-only publish
node_modules/
packages/
apps/
state/
output/
*.bak-*

# State files — crawler runtime artifacts, not part of docs
**/manifest.json
**/toc.json
.env
.env.*

# OS / editor
.DS_Store
*.swp
